defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: trust_exchange_development

test:
  <<: *defaults
  database: trust_exchange_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: trust_exchange
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
